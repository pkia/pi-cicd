[Service]
# Bind to loopback only — the tailnet must not see an unauthenticated
# Prometheus. Grafana (step 2) scrapes the same address locally.
ExecStart=
ExecStart=/usr/bin/prometheus --config.file=/etc/prometheus/prometheus.yml --web.listen-address=127.0.0.1:9090 $ARGS
